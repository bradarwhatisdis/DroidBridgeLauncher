<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions used by the launcher -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_droidbridge"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DroidBridgeLauncher">

        <!-- Splash screen / entry point -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.DroidBridgeLauncher.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main game launcher activity -->
        <activity
            android:name=".launcher.JavaLaunch"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.DroidBridgeLauncher" />

        <!-- SDL / game surface activity -->
        <activity
            android:name=".SDLActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|navigation"
            android:theme="@style/Theme.DroidBridgeLauncher.Game" />

        <!-- Settings -->
        <activity
            android:name=".LauncherSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.DroidBridgeLauncher" />

        <!-- Controls editor -->
        <activity
            android:name=".ControlsActivity"
            android:exported="false"
            android:theme="@style/Theme.DroidBridgeLauncher" />
        <activity
            android:name=".ControlsEditorActivity"
            android:exported="false"
            android:theme="@style/Theme.DroidBridgeLauncher" />

        <!-- Modpack details -->
        <activity
            android:name=".modmanager.ModpackUpdateDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.DroidBridgeLauncher" />

        <!-- Voice chat permission helper -->
        <activity
            android:name=".SimpleVoiceChatPermissionActivity"
            android:exported="false"
            android:theme="@style/Theme.DroidBridgeLauncher" />

        <!-- Error display -->
        <activity
            android:name=".ErrorActivity"
            android:exported="false"
            android:theme="@style/Theme.DroidBridgeLauncher" />

    </application>

</manifest>
